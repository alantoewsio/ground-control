Release 0.3.6: system administration (Netflow, HA), webadmin SSO, launcher update, platform polish

Version
- Bump pyproject.toml to 0.3.6 (app about/version still read from pyproject at runtime).

Firewalls · System administration (Sophos XML API)
- Netflow configuration: table/merge/update pipeline (netflow_configuration_table,
  netflow_configuration_merge, netflow_sophos_update), task-queue enqueue endpoints,
  administration page, flyout partial, gc-system-admin-netflow.js, and firewall_config_sync
  integration with cached payloads.
- HA configuration: ha_configure_table, System Services page, flyout partial,
  gc-system-services-ha.js, and API wiring from inventory sync.

Webadmin & firewall connectivity
- webadmin_sso_login for SSO-oriented webadmin sessions; webadmin_proxy and sophos_connect
  adjustments; firewall_webadmin_info refinements.

Launcher & packaging
- gc_tray_wrapper: periodic git fetch vs origin/upstream, Update control (enabled when remote
  is ahead), git pull --ff-only then scripts/build_launcher.ps1 (PowerShell / pwsh).
- build_launcher.ps1: robust stop/copy/restart of running launcher.exe instances around build.

Application core & ops
- main.py: routes and APIs for designer page, system administration, system services, HA and
  Netflow tables, and task-queue Netflow jobs; related navigation and template context.
- monitor_scheduler: immediate probe behavior; task_queue_service extensions.
- access_history, auth, config, config_viewer_tree, docker_secrets, app/__init__ startup
  (docker secrets hydration ordering).

UI / static
- templates: firewalls.html expansion, firewalls_base/network, base.html, administration and
  system services pages, designer.html, net entity macros, HA/Netflow flyouts.
- style.css, gc-network-entity.js, gc-top-bar-firewalls.js (including multiselect launch URL
  behavior).

Tests
- New: HA and Netflow tables/update, webadmin SSO, designer page, daily full firewall sync
  scheduler, firewall config sync netflow cache, inventory sync error map, monitor scheduler
  immediate probe, nav multiselect launch URLs, docker runtime, access history, sophos connect.
